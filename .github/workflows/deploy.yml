name: Deploy to Zeabur
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install Dependencies
      run: npm install
        
    - name: Build
      run: npm run build
      env:
        CI: true
        
    - name: Deploy to Zeabur
      uses: zeabur/deploy-to-zeabur@v1
      with:
        token: ${{ secrets.ZEABUR_TOKEN }}
        project: chat
        service: web
